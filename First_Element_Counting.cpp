/**
 *     DHIP
**/
#include<bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin>>t;
    while(t--)
    {
        int n;
        vector<int>v(n),v_cpy;
        for(int i = 0; i<n; i++)
        {
            cin>>v[i];
        }
        v_cpy=v;
        sort(v.begin(),v.end());
        
        map<int,int>mp;
        int i=0;
        while(i<n)
        {
            if(i==0 || i==n-1)
            {
    
            }
        }
    }
    return 0;
}